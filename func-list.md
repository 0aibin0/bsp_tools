# DisplayTools 功能清单 —— 待你修改

**怎么用（三种都行，挑顺手的）**

1. 把「保留」列改成 `否` —— 我照这个删
2. 直接删掉整行 —— 效果一样
3. 拿不准的写 `?` —— 我会说明删了会影响什么，你再定

改完发我一句「按 func-list 改」就行；也可以用编号说，例如「删 24 31 55」。
「建议」列只是我的看法，**以你的标记为准**。

版本：v3.2.0 · 常用工具 11 个模块 · 共 82 项

---

## 一、全局 / 框架

| 编号 | 位置 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- | --- |
| 1 | 全局 | 左侧边栏导航 | 6 个页面分 3 组：常用 / 调试工具 / 转换工具 | 保留 | 是 |
| 2 | 全局 | 快捷操作（8 项） | 侧边栏底部：列出设备 / 抓现场包 / 获取 root / remount / 挂载 debugfs / 启动参数 / 截图并保存 / 重启设备 | 保留 | 是 |
| 3 | 全局 | 命令面板 Ctrl+K | 搜索**命令收藏**（同一份 DisplayTools.json），回车执行；现敲的命令可存成收藏 | 保留 | 是 |
| 4 | 全局 | 快捷键 Ctrl+1~6 | 切换页面 | 保留 | 是 |
| 5 | 全局 | 浮层提示 Toast | 右下角操作结果反馈 | 保留 | 是 |
| 6 | 状态栏 | 设备状态指示 | 绿点/灰点 + 序列号（过长中间省略） | 保留 | 是 |
| 7 | 状态栏 | 多设备切换 | 下拉选设备，选定后所有命令自动带 `-s` | 保留 | 是 |
| 8 | 状态栏 | 时钟 / 作者 | 右下角 | 可删 | 是 |
| 9 | 启动 | 单实例守卫 | 重复启动激活已有窗口，不抢设备锁 | 保留 | 是 |
| 10 | 启动 | 启动自检 adb/fastboot | 找不到时状态栏给可点击提示 | 保留 | 是 |
| 11 | 设置 | 工具链路径配置 | 指定 platform-tools 目录，不依赖 PATH | 保留 | 是 |
| 12 | 设置 | 深浅色主题 | 跟随系统 / 浅色 / 深色（重启生效） | 待定 | 是 |
| 13 | 关于 | 检查更新 | 比版本号 + 直接从 GitHub 仓库下载最新 exe（Release 附件优先，否则取仓库 dist 下提交的文件） | 保留 | 是 |
| 14 | 全局 | 窗口几何 / 上次页面记忆 | 下次打开回到原样 | 保留 | 是 |
| 15 | 全局 | 崩溃日志 crash.log | 异常写文件，下次启动提示一次 | 保留 | 是 |
| 16 | 全局 | 帮助 / 更新日志 / 关于 | 顶部三个说明弹窗 | 保留 | 是 |

## 二、常用工具 · 模块级（先定模块，再往下看细节）

| 编号 | 模块 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 17 | 日志分析 | logcat / dmesg 抓取、过滤、导出 | 保留 | 是 |
| 18 | 设备信息 | 15 项属性查询 + 导出报告 | 保留 | 是 |
| 19 | 显示调试 | 背光、DCS、时序带宽计算（LCM 核心） | 保留 | 是 |
| 20 | **异常守护** | 实时盯 ESD / DSI 超时 / underrun / panic 等 7 类关键字 | **ESD 不做 → 建议整块删** | 否 |
| 21 | **现场包** | 一键抓全现场证据打包 zip | 保留 | 是 |
| 22 | 触摸调试 | 点击/滑动/按键注入、触摸可视化、getevent | 待定 | 是 |
| 23 | 电池调试 | 状态卡、采样记录、充放电控制 | 待定 | 否 |
| 24 | 系统调试 | 属性读写、CPU 调频、系统操作、12 项查询 | 保留 | 是 |
| 25 | 性能监控 | 采样、实时指标、趋势曲线、记录表 | 待定 | 是 |
| 26 | 文件管理 | 浏览设备目录、上传下载、路径书签 | 待定 | 是 |
| 27 | 命令收藏 | 命令增删改查、分组、双击执行 | 与命令面板重复，可删 | 是 |

## 三、显示调试（LCM 核心，逐项）

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 28 | 背光读取 / 设置 | settings + max_brightness，滑块 + 数值框 | 保留 | 是 |
| 29 | 分辨率应用 / 复位 | `wm size` | 保留 | 是 |
| 30 | 刷新率应用 / 读取 | 支持 fps 节点的平台 | 保留 | 是 |
| 31 | DCS 写入 | 选节点 + 写字节序列 | 保留 | 否 |
| 32 | DCS 读取 | 选节点 + 读回 | 保留 | 否 |
| 33 | **ESD 重置按钮** | DCS 卡片里的按钮 | **ESD 不做 → 可删** | 否 |
| 34 | **DCS 包构造** | 给命令+参数自动生成短包(0x05)/带参短包(0x15)/长包(0x39) | 保留 | 是 |
| 35 | **时序 / 带宽计算** | pclk、DSI 总速率、每 lane 速率、消隐占比，超 D-PHY 上限标红 | **保留（你要的）** | 是 |
| 36 | 时序参数从剪贴板解析 | 粘 spec/dmesg 片段自动填参数 | 保留 | 是 |
| 37 | 时序参数从设备读取 | 跑 `dumpsys display` 解析分辨率/刷新率 | 待定 | 是 |
| 38 | Panel 参数查询（6 按钮） | 型号/显示信息/背光节点/DSI 节点/显示属性/SurfaceFlinger | 与命令面板重复，可删 | 否 |

## 四、异常守护（整块 = 编号 20，删了下面 3 项自动消失）

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 39 | 7 类关键字规则 | ESD/复位、DSI 超时欠载、显示异常、内核崩溃、内存不足、存储 IO、进程崩溃 | 可删 | 否 |
| 40 | 4 种实时来源 | dmesg -w / logcat / logcat crash / logcat 只看错误 | 可删 | 是 |
| 41 | 命中记录表 | 相同异常自动合并计数、限流提示、可导出 | 可删 | 否 |

## 五、现场包

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 42 | 18 个抓取项（可勾选） | 属性/dmesg/logcat(含 crash)/dumpsys display·SF·battery·input/中断/挂载/进程/显示节点/背光值/GPIO/分辨率密度 | 保留 | 是 |
| 43 | 打包 zip + meta.txt | 文件名带型号+序列号+时间；meta 记录每条命令的退出码与耗时 | 保留 | 是 |
| 44 | 备注拼进文件名 | 例如「闪屏」「花屏」 | 保留 | 是 |
| 45 | 每台在线设备各抓一份 | 多设备工位批量 | 待定 | 是 |

## 六、触摸调试

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 46 | 点击注入 | 坐标 + 中心/四角快捷位置 | 待定 | 是 |
| 47 | 滑动注入 | 起止坐标 / 上下左右 / 时长 | 待定 | 是 |
| 48 | 按键注入 | 返回/主页/菜单/电源/音量±/唤醒 + 任意键码 | 待定 | 是 |
| 49 | 触摸可视化开关 | show_touches / pointer_location 开·关·读状态 | 待定 | 是 |
| 50 | getevent 抓取 | 抓 5 秒事件、只看 ABS_MT | 待定 | 是 |
| 51 | 输入设备列表 / 触摸属性 | `dumpsys input`、输入设备枚举 | 待定 | 否 |

## 七、电池调试

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 52 | 8 项状态卡 | 电量/健康/温度/电压/电流/充电状态/AC/USB | 待定 | 是 |
| 53 | 采样记录表 | 每次刷新插一行，最多 200 条 | 待定 | 是 |
| 54 | 充放电控制 | 恢复充电 / 模拟放电 / 测试模式 | 待定 | 是 |
| 55 | 定时自动刷新 | 每 5 秒 | 待定 | 是 |

## 八、系统调试

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 56 | 属性读 / 写 / 删 | `getprop` / `setprop` | 保留 | 是 |
| 57 | CPU 调频 | 策略切换、最大频率设置 | 待定 | 是 |
| 58 | 系统操作 | 重启 / bootloader / recovery / remount / debugfs / system 读写 | 保留 | 是 |
| 59 | 12 项信息查询 | 属性/CPU/内存/进程 TOP/挂载/存储/SELinux/内核/运行时长/模块 | 保留 | 是 |

## 九、性能监控

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 60 | 采样与实时指标 | CPU/内存/温度/电量/帧率/Data/掉帧 共 7 张卡 | 待定 | 是 |
| 61 | 趋势曲线 | 最多 180 个采样点，自绘 | 纯展示，可删 | 是 |
| 62 | 采样记录表 | 逐行记录，可清空 | 待定 | 是 |

## 十、文件管理

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 63 | 浏览设备目录 | `ls -la` 解析成列表，可进目录 | 待定 | 是 |
| 64 | 上传 / 下载 | `adb push` / `pull` | 待定 | 是 |
| 65 | 路径书签 | 常用设备路径收藏 | 待定 | 是 |

## 十一、命令收藏

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 66 | 命令增删改查 | 存本地 JSON | 与命令面板重复，可删 | 是 |
| 67 | 分组 / 搜索 | 按分组筛选 | 同上 | 是 |
| 68 | 双击执行 | 直接跑，输出进本页面板 | 同上 | 是 |

## 十二、Shell Tools（设备控制主界面）

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 69 | adb 常用（9 按钮） | devices / root / remount / debugfs / reboot / wm size / cmdline / dumpsys / device info | 保留 | 是 |
| 70 | Download Mode | UNISOC autoloader / QCOM EDL | 保留 | 是 |
| 71 | Debug 自定义命令 | 流式输出 + 10 条历史回放 | 保留 | 是 |
| 72 | push / pull | 带设备路径历史 | 保留 | 是 |
| 73 | I2C/SPI、density/dpi、printk 读写 | 调试小工具集合 | 保留 | 是 |
| 74 | fastboot flash | 分区选择 + 镜像路径 + Flash(erase+flash) + bootloader/reboot | 保留 | 是 |
| 75 | func 功能键 | 电源键 / 截图并拉取 / 录屏开始·停止 / 背光 / GPIO 查询 | 保留 | 是 |
| 76 | ylog 导出 | 保存路径 + Project + Sub-Name | 与内部流程绑定，待定 | 是 |
| 77 | APK 安装 | 默认路径是本机硬编码 | 待定 | 是 |
| 78 | scrcpy 投屏 | 默认路径是本机硬编码 | 待定 | 是 |
| 79 | 右侧 Log 面板 | 竖向独占，复制 / 清空 | 保留 | 是 |

## 十三、转换工具

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 80 | Initcode 正向生成 | 4 种格式：WriteAddr / Write(Command) / Rxx / GEN_WR | 保留 | 是 |
| 81 | **Initcode 反向解析** | 字节序列 → DCS 命令清单 → 转回 4 种格式 | 保留 | 是 |
| 82 | LK ↔ Kernel 互转 + LK→BAT | 三个转换页面 | 待定 | 是 |

## 十四、工程（不是用户功能，影响维护成本）

| 编号 | 功能 | 说明 | 建议 | 保留 |
| --- | --- | --- | --- | --- |
| 83 | 一键自查 `run_checks.bat` | 14 项检查（含真机那部分要加 device 参数） | 保留 | 是 |
| 84 | 专项 check 脚本 | selftest 117 项 + theme/timing/guard/multidevice/initcode/sitepack | 保留 | 是 |
| 85 | 布局检查 | check_layout（5 种尺寸）+ inspect_ui（压缩检测） | 保留 | 是 |
| 86 | 打包 | 单文件 exe（约 36MB）+ onedir 目录版（秒开） | 保留 | 是 |

---

## 如果你只想砍一刀，我推荐这 6 项

按性价比排序（都是「删了不影响屏体调试主流程」的）：

| 优先 | 编号 | 项目 | 理由 |
| --- | --- | --- | --- |
| 1 | 20（连带 39-41） | 异常守护整块 | 你已明确 ESD 不做；剩下 6 类和日志分析/现场包重叠 |
| 2 | 27（连带 66-68） | 命令收藏模块 | 和 Ctrl+K 命令面板重复 → **v3.2.13 改成两处共用一份数据**，见下 |
| 3 | 38 | Panel 参数查询卡片 | 6 个命令按钮，命令面板里都能搜到 |
| 4 | 33 | ESD 重置按钮 | ESD 不做 |
| 5 | 61 | 性能监控的趋势曲线 | 表格已经在，曲线是纯展示 |
| 6 | 26（连带 63-65） | 文件管理模块 | Shell Tools 已有 push/pull |

---

## 本次已执行（v3.2.14）

| 项 | 处理 |
| --- | --- |
| — | 本机文件三合一：`config.ini` + `favorites.json` + `path_bookmarks.json` → 一个 `DisplayTools.json`（`config` / `commands` / `path_bookmarks` 三段）；老文件首次运行自动迁移并改名成 `*.migrated` |
| 27 / 66-68 | 收藏的增删改立即写回文件；修掉「全删光后重启又变回默认」（空数组不再被当成没数据）；写盘失败会弹提示，不再静默吞掉 |
| — | 「设置」里能看到数据文件的完整路径 + 各段条目数，带「打开所在文件夹 / 复制路径」 |

## 本次已执行（v3.2.13）

| 项 | 处理 |
| --- | --- |
| 3 / 13 | 命令面板 Ctrl+K 不再读硬编码的 18 条，改成读本机数据文件（`command_store.py`）；面板里能搜、能执行、能「存为收藏」写盘 |
| 27 / 66-68 | 命令收藏页改成同一份数据的「管理界面」（增删改、分组、搜索、复位）；两处永远一致，同一条命令不会收两遍 |
| 13 | 检查更新：比版本号之外，**可以直接下载最新 exe**（Release 附件优先，没有 Release 就从仓库 `bsp_tools/dist` 取提交好的文件），下到 exe 同目录、文件名带版本号 |
| — | exe 统一叫 `DisplayTools.exe`（不带版本号）；删掉 6 个带版本号的旧 spec |

---

## 本次已执行（v3.2.1）

按你标成 `否` 的项改完了，模块 **11 → 9** 个：

| 编号 | 项目 | 处理 |
| --- | --- | --- |
| 20（连带 39、41） | 异常守护整块 | **从页面下线**，代码文件保留（`bsp_tools/tools_guard.py`），一行就能恢复 |
| 23 | 电池调试整块 | **从页面下线**，代码文件保留（`bsp_tools/tools_battery.py`），同上 |
| 31 / 32 | DCS 写入 / 读取节点 | 删除。屏体写节点改走 Shell Tools 自定义命令 |
| 33 | ESD 重置按钮 | 删除 |
| 38 | Panel 参数查询（6 按钮） | 删除，Ctrl+K 命令面板可替代 |
| 51 | 输入设备列表 / 触摸属性 | 删除那组查询按钮，「抓取事件」保留 |

**两处要你确认的矛盾**（我按模块级标记执行了）：
- 编号 40「4 种实时来源」标的是 `是`，但它属于已删除的异常守护模块 → **一并去掉**了
- 编号 52-55（电池状态卡/采样表/充放电控制/自动刷新）标的是 `是`，
  但所属模块 23 标的是 `否` → **整块去掉**了

如果电池调试其实要留（那 4 项你都打了是），说一声，我把模块加回去就行。

**顺带改掉的**：
- DCS 卡片改成纯「DCS 包构造」：命令 + 参数 → 生成字节序列 + 一键复制，不再往设备写
- 背光卡片里「需要直接写节点时请用下方 DCS 写入」这句过时提示改成了新流程
- 常用工具副标题、帮助、更新日志、ReadMe 全部同步成 9 个模块
- 自测从 117 项调整为 110 项（删掉电池相关用例，新增「已下线模块不得再出现」
  和「DCS 构造不产生任何设备命令」等断言）
- 新增 `devtools/check_wraplabel.py`：专抓「自动换行标签被压得显示不全」
  ——这是 inspect_ui 抓不到的盲区（wordWrap 的 minimumSizeHint 按最窄换行算）

